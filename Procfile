# This file is is used to define process types for deployment, especially on platforms like Heroku. It tells the platform how to start the web application and any background workers.

web: otree prodserver1of2
# Starts the main oTree web server process

worker: otree prodserver2of2
# Starts a secondary oTree worker process (for background tasks or scaling)
